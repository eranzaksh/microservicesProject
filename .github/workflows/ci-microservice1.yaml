name: Microservice1 CI Build & Push to Docker Hub

on:
  push:
    branches: [ main ]
    paths:
      - 'microservice1/**'    # change to microservice2/** in the other workflow

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1) Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2) Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 3) Build & push
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./microservice1
          file:    ./microservice1/Dockerfile
          push:    true
          tags:    eranzaksh/microservice1:latest, eranzaksh/microservice1:${{ github.sha }} 